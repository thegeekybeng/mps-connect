{
  "name": "mps-ai-proxy",
  "version": "1.0.0",
  "description": "Server-side AI proxy for MPS Connect — system prompt never leaves this container",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cookie-parser": "^1.4.7",
    "express": "^4.18.2",
    "jose": "^5.0.0"
  },
  "engines": {
    "node": ">=24"
  }
}
